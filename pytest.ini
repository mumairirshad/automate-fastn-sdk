[pytest]
testpaths = tests
asyncio_mode = auto
markers =
    cli: CLI command tests
    auth: Authentication tests
    connector: Connector management tests
    flow: Flow management tests
    kit: Connect Kit tests
    skill: Skills tests
    agent: Agent tests
addopts = -v --tb=short
